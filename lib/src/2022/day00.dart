import '../utils.dart';

int day0star1(Input input) => _processInput(input).length;

int day0star2(Input input) => _processInput(input).length;

List<String> _processInput(Input input) => input.getLines();
