import '../utils.dart';

Iterable<String> _processInput(String input) => input.lines;

int day1star1(String input) => _processInput(input).length;

int day1star2(String input) => _processInput(input).length;
